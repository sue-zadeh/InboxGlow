services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: inboxglow
      POSTGRES_PASSWORD: inboxglow
      POSTGRES_DB: inboxglow
    ports:
      - "5432:5432"
    volumes:
      - inboxglow_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U inboxglow"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  inboxglow_data:
